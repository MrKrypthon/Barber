// Colores del mockup original (docs/Propuesta.pdf, "Barbería El Andaluz")
// para cuando el negocio todavía no eligió los suyos — mismos valores que
// :root en globals.css. Un solo lugar para no repetirlos en cada archivo
// que necesita un fallback (ThemeVars, ConfigView, exportación a PDF...).
export const DEFAULT_PRIMARY_COLOR = "#24406B";
export const DEFAULT_SECONDARY_COLOR = "#C0392B";
export const DEFAULT_BACKGROUND_COLOR = "#F5F1E8";
